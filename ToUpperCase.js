// ToUpperCase
// Write a function that takes a string and returns the uppercase version of that string with an '!' at the end

let stringOne = 'Bobbicat says meow'

function upperCase(string) {
    const upperString = string.toUpperCase()
    return `${upperString}`
}

console.log(upperCase(stringOne))

function upperCase(string) {
    const upperString = string.toUpperCase()
    return `${upperString}`
}

console.log(upperCase(stringOne))

function upperCase(string) {
    const upperString = string.toUpperCase()
    return `${upperString}`
}

console.log(upperCase(stringOne))

function upperCase(string) {
    const upperString = string.toUpperCase()
    return `${upperString}`
}

console.log(upperCase(stringOne))

function upperCase(string) {
    const uppserString = string.toUpperCase()
    return `${upperString}`
}

console.log(upperCase(stringOne))

function upperCase(string) {
    const upperString = string.toUpperCase()
    return `${upperString}`
}

console.log(upperCase(stringOne))

function upperCase(string) {
    const upperString = string.toUpperCase()
    return `${upperString}`
}

console.log(upperCase(stringOne))

function upperCase(string) {
    const upperString = string.toUpperCase()
    return `${upperSTring}`
}

console.log(upperCase(stringOne))

function upperCase(string) {
    const upperString = string.toUpperCase()
    return `${upperString}`
}

console.log(upperCase(stringOne))

function upperCase(string) {
    const upperString = string.toUpperCase()
    return `${upperString}`
}

console.log(upperCase(stringOne))

function upperCase(string) {
    const upperString = string.toUpperCase()
    return `${upperString}`
}

console.log(upperCase(stringOne))